<view class="container">
  <!-- Loading State -->
  <view wx:if="{{isLoading}}" class="loading-container">
    <text class="loading-text">加载中...</text>
  </view>

  <!-- Error State -->
  <view wx:elif="{{error}}" class="error-container">
    <text class="error-text">{{error}}</text>
    <button class="btn-back" bindtap="onBack">返回</button>
  </view>

  <!-- Exercise Detail -->
  <view wx:else-if="{{exercise}}" class="exercise-detail">
    <!-- Header -->
    <view class="header">
      <button class="btn-back" bindtap="onBack">返回</button>
      <text class="title">{{exercise.name}}</text>
      <view class="placeholder"></view>
    </view>

    <!-- Basic Info Card -->
    <view class="card info-card">
      <view class="info-row">
        <text class="info-label">主肌肉群</text>
        <text class="info-value primary">{{getPrimaryMuscle(exercise)}}</text>
      </view>
      <view class="info-row">
        <text class="info-label">目标肌肉</text>
        <text class="info-value">{{getTargetMuscles(exercise)}}</text>
      </view>
      <view class="info-row" wx:if="{{exercise.category}}">
        <text class="info-label">类别</text>
        <text class="info-value">{{exercise.category}}</text>
      </view>
      <view class="info-row" wx:if="{{exercise.equipment}}">
        <text class="info-label">器械</text>
        <text class="info-value">{{exercise.equipment}}</text>
      </view>
      <view class="info-row" wx:if="{{exercise.difficulty}}">
        <text class="info-label">难度</text>
        <text class="info-value">{{exercise.difficulty}}</text>
      </view>
    </view>

    <!-- Steps Card -->
    <view class="card steps-card" wx:if="{{getSteps(exercise).length > 0}}">
      <text class="section-title">动作步骤</text>
      <view class="steps-list">
        <view wx:for="{{getSteps(exercise)}}" wx:key="index" class="step-item">
          <text class="step-number">{{index + 1}}</text>
          <text class="step-text">{{item}}</text>
        </view>
      </view>
    </view>

    <!-- Related Exercises -->
    <view class="card related-card" wx:if="{{relatedExercises.length > 0}}">
      <text class="section-title">相关动作</text>
      <view class="related-list">
        <view
          wx:for="{{relatedExercises}}"
          wx:key="id"
          class="related-item"
          data-id="{{item.id}}"
          bindtap="onRelatedExerciseTap"
        >
          <text class="related-name">{{item.name}}</text>
          <text class="related-arrow">></text>
        </view>
      </view>
    </view>
  </view>

  <!-- Empty State -->
  <view wx:else class="empty-container">
    <text class="empty-text">动作不存在</text>
    <button class="btn-back" bindtap="onBack">返回</button>
  </view>
</view>