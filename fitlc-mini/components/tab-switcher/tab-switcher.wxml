<view class="tab-switcher">
  <view
    wx:for="{{tabs}}"
    wx:key="key"
    class="tab-item {{current === item.key ? 'active' : ''}}"
    data-tab="{{item.key}}"
    bindtap="onTabTap"
  >
    <text>{{item.label}}</text>
  </view>
</view>