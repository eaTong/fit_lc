<view class="page-container">
  <!-- Loading -->
  <view wx:if="{{isLoading}}" class="loading-container">
    <text class="loading-text">加载中...</text>
  </view>

  <!-- Error -->
  <view wx:elif="{{error}}" class="error-container">
    <text class="error-text">{{error}}</text>
  </view>

  <!-- Plan Content -->
  <view wx:elif="{{plan}}" class="plan-content">
    <!-- Plan Header Card -->
    <view class="plan-header-card">
      <view class="plan-title-row">
        <text class="plan-name">{{plan.name}}</text>
        <view class="status-badge" style="background-color: {{plan.status === 'active' ? '#4CAF50' : plan.status === 'completed' ? '#2196F3' : '#888888'}}">
          <text class="status-text">{{getStatusLabel(plan.status)}}</text>
        </view>
      </view>

      <!-- Plan Meta Grid -->
      <view class="plan-meta-grid">
        <view class="meta-item">
          <text class="meta-label">目标</text>
          <text class="meta-value">{{getGoalLabel(plan.goal)}}</text>
        </view>
        <view class="meta-item">
          <text class="meta-label">频率</text>
          <text class="meta-value">每周{{plan.frequency}}次</text>
        </view>
        <view class="meta-item">
          <text class="meta-label">周期</text>
          <text class="meta-value">{{plan.duration_weeks}}周</text>
        </view>
        <view class="meta-item">
          <text class="meta-label">经验</text>
          <text class="meta-value">{{getExperienceLabel(plan.experience)}}</text>
        </view>
      </view>

      <!-- Equipment -->
      <view wx:if="{{plan.equipment}}" class="equipment-row">
        <text class="meta-label">器械：</text>
        <text class="equipment-tags">{{plan.equipment}}</text>
      </view>
    </view>

    <!-- Progress Stats (for active plans) -->
    <view wx:if="{{plan.status === 'active' && analysis}}" class="stats-card">
      <view class="stats-title">执行进度</view>
      <view class="stats-grid">
        <view class="stat-item">
          <text class="stat-value">{{getCompletionRate()}}%</text>
          <text class="stat-label">完成率</text>
        </view>
        <view class="stat-item">
          <text class="stat-value">{{getCompletedWorkouts()}}</text>
          <text class="stat-label">已完成</text>
        </view>
        <view class="stat-item">
          <text class="stat-value">{{getTotalWorkouts()}}</text>
          <text class="stat-label">总训练</text>
        </view>
      </view>
    </view>

    <!-- Week Days Schedule -->
    <view class="schedule-section">
      <text class="section-title">每周训练安排</text>
      <view class="week-days">
        <view
          wx:for="{{weekDays}}"
          wx:key="dayIndex"
          class="day-card"
        >
          <view class="day-header">
            <text class="day-label">{{item.day}}</text>
            <text class="exercise-count">{{item.exercises.length}}个动作</text>
          </view>
          <view class="exercise-list">
            <view
              wx:for="{{item.exercises}}"
              wx:for-item="exercise"
              wx:key="id"
              class="exercise-item"
            >
              <text class="exercise-name">{{exercise.name}}</text>
              <text class="exercise-info">{{formatExerciseInfo(exercise)}}</text>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- Empty Days State -->
    <view wx:if="{{weekDays.length === 0}}" class="empty-state">
      <text class="empty-icon">📅</text>
      <text class="empty-title">暂无训练安排</text>
      <text class="empty-desc">该计划还没有分配训练动作</text>
    </view>
  </view>

  <!-- Empty Plan -->
  <view wx:elif="{{!plan && !isLoading}}" class="empty-state">
    <text class="empty-icon">📋</text>
    <text class="empty-title">计划不存在</text>
    <text class="empty-desc">无法找到该训练计划</text>
  </view>

  <!-- Bottom Actions -->
  <view wx:if="{{plan}}" class="bottom-action">
    <!-- Draft plan: show activate button -->
    <view wx:if="{{plan.status === 'draft'}}" class="activate-btn" bindtap="onActivate">
      <text class="activate-btn-text">激活计划</text>
    </view>

    <!-- Active plan: show start execute button -->
    <view wx:if="{{plan.status === 'active'}}" class="start-btn" bindtap="onStartExecute">
      <text class="start-btn-text">开始执行</text>
    </view>

    <!-- Completed plan: show back button -->
    <view wx:if="{{plan.status === 'completed'}}" class="outline-btn" bindtap="goBack">
      <text class="outline-btn-text">返回</text>
    </view>
  </view>
</view>