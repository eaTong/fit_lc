# F-004 一键记录模式设计

**创建时间：** 2026-04-26
**功能ID：** F-004
**状态：** 设计完成（已更新后端转文字方案）

---

## 一、概述

**功能名称：** 一键记录模式（语音优先）
**功能描述：** 在现有聊天界面底部增加"按住说话"按钮，用户录音后松开，音频发送到后端 `/api/voice/transcribe` 转文字，再调用 `/api/chat` 解析，保存成功时插入 F-001 视觉卡片。

**优先级：** P0（L1 基础功能层）
**依赖关系：** 无依赖，可最先开发

---

## 二、核心交互

### 2.1 主要流程

```
用户按住麦克风 → 录音(WAV) → 松开 → 上传至 /api/voice/transcribe
                                                      ↓
                                              后端 MiniMax 转文字
                                                      ↓
                                              返回文字 → 前端调 /api/chat
                                                            ↓
                                              Agent 解析 → save_workout 工具
                                                            ↓
                                              返回 reply + savedData
                                                            ↓
                                              前端检测 savedData → 显示视觉卡片 + 播放音效
```

### 2.2 支持的交互节奏

**一句完成（核心实现）：**
- 语音输入 → 松开 → 直接保存 → 显示视觉卡片 → 播放成功音效
- 适用场景：简单明确的训练记录（如"深蹲100公斤5组8个"）

**对话式（后续扩展）：**
- 说"今天练了" → AI 追问"练了什么？" → 用户继续说 → AI 保存

**连续记录（后续扩展）：**
- 可多次说动作，最后说"保存"统一确认

---

## 三、技术架构

### 3.1 数据流

```
麦克风 → 录音(WAV格式) → 松开即上传 → /api/voice/transcribe
                                                     ↓
                                           后端 MiniMax 转文字
                                                     ↓
                                           返回文字 → 前端调 /api/chat
                                                         ↓
                                               LangChain Agent → save_workout
                                                         ↓
                                               reply + savedData → 前端显示视觉卡片
```

### 3.2 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端录音 | MediaRecorder API | 录制 WAV 格式音频 |
| 后端转文字 | MiniMax API | 复用现有 API Key，/v1/audio/speech 接口 |
| 后端接口 | /api/voice/transcribe | 新增，专门处理语音转文字 |
| 业务处理 | 复用 /api/chat | 文字解析和保存 |
| AI解析 | 复用现有 Agent | LangChain + save_workout tool |
| 视觉反馈 | F-001 视觉卡片组件 | 已规划功能 |

---

## 四、新增接口

### 4.1 POST /api/voice/transcribe

**功能：** 接收音频文件，调用 MiniMax 转文字

**认证：** JWT Token（通过 authMiddleware）

**请求：**
- Content-Type: multipart/form-data
- 参数: audio (WAV 文件)

**响应：**
```json
{
  "text": "深蹲100公斤5组8个",
  "success": true
}
```

**错误响应：**
```json
{
  "error": "转写失败",
  "success": false
}
```

### 4.2 前端处理流程

```
录音完成 → POST /api/voice/transcribe → 获取文字 → POST /api/chat → 获取 reply + savedData → 显示视觉卡片
```

---

## 五、状态管理

| 状态 | UI表现 | 说明 |
|------|--------|------|
| idle | 麦克风按钮可点击，文字"按住说话" | 空闲状态 |
| recording | 按钮变红+波形动画，文字"松开结束" | 录音中 |
| processing | 按钮禁用，显示加载动画 | 解析中 |
| success | 显示视觉卡片，播放成功音效 | 成功保存 |
| partial | 显示部分数据，标记"待补充" | 部分成功 |
| failed | 显示错误提示，可重试 | 保存失败 |

---

## 六、接口契约（复用 /api/chat）

### 6.1 请求

```json
POST /api/chat/message
{
  "message": "深蹲100公斤5组8个",
  "historyMessages": []
}
```

### 6.2 响应

```json
{
  "reply": "已记录你的深蹲训练 💪\n\n100kg × 5组 × 8个",
  "savedData": {
    "type": "workout",
    "id": 123,
    "exercises": [{...}]
  }
}
```

**判断逻辑：** 当 `savedData` 存在且 `savedData.type === 'workout'` 时，前端显示 F-001 视觉卡片。

---

## 七、解析失败处理

### 7.1 部分可识别

- 保存能解析的部分（如只识别出"深蹲"但缺少重量）
- 数据标记为"待补充"状态
- 显示可编辑卡片，用户可手动补充

### 7.2 完全无法识别

- 不保存任何数据
- 显示"没听清，请再说一次"
- 提供重试按钮

---

## 八、前端实现要点

1. **录音组件：** 使用 MediaRecorder API 录制 WAV 格式
2. **状态管理：** 使用 React useState 管理 idle/recording/processing/success/partial/failed
3. **自动发送：** 转文字完成后自动调 /api/chat
4. **视觉卡片：** 复用 F-001 的视觉卡片组件，通过 savedData 驱动显示
5. **音效反馈：** 成功时播放简短音效（可使用 Web Audio API）

---

## 九、后端改动

1. **新增接口：** `/api/voice/transcribe` - 接收音频，转写为文字
2. **集成 MiniMax：** 调用 MiniMax 语音转文字 API（复用现有 API Key）
3. **复用接口：** `/api/chat/message` 保持不变

---

## 十、测试场景

| 场景 | 输入 | 预期结果 |
|------|------|----------|
| 简单记录 | "深蹲100公斤5组8个" | 保存成功，显示视觉卡片 |
| 部分识别 | "练了腿" | 保存部分，标记待补充 |
| 听不懂 | "啦啦啦" | 提示重试，不保存 |
| 连续动作 | "深蹲100公斤" + "卧推80公斤" | 分两条记录保存 |

---

## 十一、后续扩展

- 对话式：AI 追问补全信息
- 连续记录：多条动作统一确认
- 离线支持：本地缓存语音，网络恢复后上传

---

## 十二、关联功能

| 功能ID | 功能名称 | 关系 |
|--------|---------|------|
| F-001 | 保存成功视觉卡片 | 显示载体 |
| F-006 | 撤销后重新记录 | 撤销后可使用语音重新记录 |
| F-017 | 你可能想提示 | 关键时刻的语音入口 |

---

## 十三、状态

- [x] 设计完成
- [x] 已更新后端转文字方案（2026-04-26）
- [ ] 待用户确认
- [ ] 写入实现计划